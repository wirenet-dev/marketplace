# Valid Workspace Fixture - Agent Entry Point

Read `README.md`, then `identity/context.md`, `identity/preferences.md`, and
`identity/rules.md`.

The agent may read across the system for relevant context, but may write or edit
only inside this workspace unless the user explicitly says otherwise.
