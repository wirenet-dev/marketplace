interface:
  display_name: "Workspace Agent"
  short_description: "Create workspace agent personas"
  default_prompt: "Use $workspace-agent to create an agent persona."

policy:
  allow_implicit_invocation: true

