interface:
  display_name: "Workspace Lint"
  short_description: "Run deterministic workspace checks"
  default_prompt: "Use $workspace-lint to validate this workspace."

policy:
  allow_implicit_invocation: true

